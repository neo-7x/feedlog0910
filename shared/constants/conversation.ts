// How long a visitor can still see a widget conversation. A visibility window,
// not a deletion job: rows stay, reads stop returning them.
export const CONVERSATION_RETENTION_DEFAULT_DAYS = 14
export const CONVERSATION_RETENTION_MIN_DAYS = 7
export const CONVERSATION_RETENTION_MAX_DAYS = 3650
